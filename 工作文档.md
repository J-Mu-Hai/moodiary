# Moodsonder 工作文档

## 项目信息
- 项目路径：`D:\moodsonder`
- Flutter：3.29.0 / `C:\tools\flutter\bin\flutter.bat`
- 桌面快捷方式：`D:\桌面\Moodsonder.lnk`（指向 `mood_diary.exe`）

## 编译方式

### 增量编译（改 Dart 代码后）
```bash
taskkill /f /im mood_diary.exe
C:\tools\flutter\bin\flutter.bat build windows --release
cp build/windows/app.so build/windows/x64/runner/Release/data/
cp -r build/flutter_assets build/windows/x64/runner/Release/data/
```

### 常见构建问题
- `cpp_client_wrapper/*.cc` 找不到 → 从 Flutter SDK 复制
  ```bash
  cp C:/tools/flutter/bin/cache/artifacts/engine/windows-x64/cpp_client_wrapper/*.cc windows/flutter/ephemeral/cpp_client_wrapper/
  ```
- `mood_diary.exe` 被锁定 → 杀掉进程
  ```bash
  taskkill /f /im mood_diary.exe
  ```

## 已实现功能

### 1. 固定分类
- **文件**：`lib/common/values/fixed_categories.dart`
- 分类：任务管理、日记、每日计划、觉醒时刻
- 特点：不可删除、带锁图标
- 启动时自动创建（`IsarUtil.ensureFixedCategories()` 在 `main.dart` 启动时调用）

### 2. 拖拽排序
- **文件**：`lib/pages/category_manager/`（view + logic + state）
- 分类管理页面使用 `ReorderableListView.builder`
- 长按左侧 ⋮⋮ 手柄拖拽

### 3. AI 多 Provider
- **文件**：`lib/common/models/ai_provider.dart`
- `AIProvider` 抽象接口
- `TencentProvider`（腾讯混元）
- `OpenAICompatibleProvider`（OpenAI / DeepSeek 等）
- 实验室页面可配置多个 Provider

### 4. # 空格标题快捷输入
- **文件**：`lib/pages/edit/edit_logic.dart` → `_checkHeadingShortcut()`
- 行首输入 `# ` `## ` `### ` `#### ` 自动转为对应级别标题

### 5. 悬浮目录面板
- **文件**：`lib/pages/edit/edit_view.dart` → `_OutlinePanel` 组件
- 富文本编辑器工具栏 📋 按钮 → 悬浮在编辑器上的目录
- 可拖拽、可最小化、可关闭
- 点击标题跳转到文档对应位置

### 6. AI 日记感知
- **文件**：`lib/pages/assistant/assistant_logic.dart` → `_buildDiaryContext()`
- 智能助手页面 📖 开关 → AI 可读取最近日记摘要
- 每次对话自动注入近 7 天日记作为上下文

### 7. AI 框架层（服务层，待接入 UI）
- `lib/services/ai_functions.dart` — 5 个数据查询函数
- `lib/services/ai_prompt_manager.dart` — 从 assets 加载提示词
- `lib/services/ai_triggers.dart` — 触发器引擎（含冷却）
- `lib/services/output_splitter.dart` — 拆句输出

### 8. AI 配置文件（assets/ai/）
- `personality.json` — AI 性格设定
- `functions.json` — 函数定义
- `triggers.json` — 触发器定义
- `prompts/` — 所有提示词文件（可单独编辑）

## 待解决问题

### 第一次对话请求失败
- **原因**：`toBottom()` 在列表未渲染时调用 `scrollController.position` 抛出异常
- **修复**：已加 `hasClients` 检查

### `cpp_client_wrapper/*.cc` 文件缺失
- **原因**：`flutter clean` 或 cmake 配置时删除 ephemeral 目录
- **修复**：从 Flutter SDK 复制（见上方命令）

### AI 触发器未接入 UI
- 触发器引擎已实现，但尚未接入界面
- 需要：定时检查 + 闲置检测 + 推送通知

## 后续开发方向
1. 将 AI 性格提示词注入到现有对话（修改 `assistant_logic.dart`）
2. 将触发器引擎连接到定时器 + 事件监听
3. 触发器触发后的消息推送到对话界面
4. 用户配置面板（开关各触发器）


# 2026年8月5日
我觉得继续我的工作
## 我意识到的问题
1、ai对话的细节有问题，一是发送按键shift+enter和enter没有区别了
2、我需要让这个ai分为三个层次，一个是约束性质的prompt,一个是通用价值观念，一个是用户的私档案。我需要这三个东西出现在同一个比较集中的地方。
3、我需要一些列可靠的问题去测试我的功能是否能够像我设想的一样合理
4、我需要让这个ai的回答能够分块，以及像微信这样的对方输入中，这样更加模拟是网友的感觉

# 2026年8月6日
我承认这个软件确实有很多细节地方，可以按照我希望的方式进行优化的。

说起来容易，做起来确实很难。我虽然知道要先整体规划，但是执行起来，还是总是陷入小地方修改的死穴
1、我需要完善任务规划和ai修改的逻辑
2、我需要完善每日计划和ai评价的逻辑
3、我需要完善ai分析的逻辑和方式
4、我需要思考这些数据如何和ai对于用户的画像进行分析。
5、无可否认的是什么，这东西就是一个通过廉价api源源不断添油加醋修改出来的怪物。
6、我怕的是什么，是我总是加入一个功能之后，另外一个功能就莫名其妙没有对接上